#include<stdio.h>
float f(float x){
    return 2*(x*x*x) + 3*(x*x);
}
void simp(){
    int n;
    float a,b,h,traps;
    printf("Enter the values of a & b: ");
    scanf("%f %f", &a,&b);
}
int main(){

return 0;
}